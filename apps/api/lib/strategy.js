/**
 * Stub for computing trade signals.
 * Returns an array of { symbol, side: 'buy'|'sell', qty }.
 */
export function computeSignals(marketData, news) {
  // e.g. no trades by default
  return [];
}

